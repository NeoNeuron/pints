// Replaced in Phase 3.
export async function mountScheduleTab(host) {
  host.innerHTML = `<p class="msg warn">The schedule editor lands in Phase 3.
    Until then, schedule items can be added directly in the Firebase console.</p>`;
}
